# fastCutVid

@AGENTS.md

For operating the cutter, read `docs/LOCAL_AGENTS.md` and `docs/AGENT_GUIDE.md`.
The shared timeline skill is `.agents/skills/fast-cut-timelines/SKILL.md`;
read it directly when working on a timeline. It is not installed as a Claude slash command.
Use the downloaded executable for validation/rendering, or Cargo when working from source.
